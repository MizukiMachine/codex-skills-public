interface:
  display_name: "Grounded Agent Design"
  short_description: "情報境界の設計＋Direct→Censor→Correct 制御ループでLLM出力を根拠ベースに保つ"
  default_prompt: "Use $grounded-agent-design to design information boundaries (per-viewer projection of authoritative state) and wrap each generation in a Direct→Censor→Correct control loop so the output stays grounded, leak-free, and on-task."
